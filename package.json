{
  "name": "node-ts-api-template",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --ignore-watch node-modules --transpile-only src/server.ts",
    "build": "rimraf ./dist && tsc"
  },
  "devDependencies": {
    "@types/node": "^15.12.0",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
